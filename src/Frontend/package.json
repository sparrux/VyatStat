{
  "name": "vyatka-frontend",
  "private": true,
  "scripts": {
    "ng": "ng",
    "build": "ng build identity-app && ng build tracker-app",
    "build:all": "ng build auth && ng build ui && ng build identity-app && ng build tracker-app",
    "build:identity": "ng build identity-app",
    "build:tracker": "ng build tracker-app",
    "build:libs": "ng build auth && ng build ui",
    "start:identity": "ng serve identity-app",
    "start:tracker": "ng serve tracker-app",
    "dev:identity": "npm run start:identity",
    "dev:tracker": "npm run start:tracker",
    "test:identity": "ng test identity-app",
    "test:tracker": "ng test tracker-app"
  },
  "packageManager": "npm@11.13.0",
  "dependencies": {
    "@angular/animations": "^22.0.2",
    "@angular/cdk": "^22.0.2",
    "@angular/common": "^22.0.0",
    "@angular/compiler": "^22.0.0",
    "@angular/core": "^22.0.0",
    "@angular/forms": "^22.0.0",
    "@angular/platform-browser": "^22.0.0",
    "@angular/router": "^22.0.0",
    "@vyatka-tracker/ui": "file:vyatka-tracker-ui",
    "rxjs": "~7.8.0",
    "tslib": "^2.3.0"
  },
  "devDependencies": {
    "@angular/build": "^22.0.4",
    "@angular/cli": "^22.0.2",
    "@angular/compiler-cli": "^22.0.0",
    "jsdom": "^28.0.0",
    "ng-packagr": "^22.0.0",
    "prettier": "^3.8.1",
    "typescript": "~6.0.2",
    "vitest": "^4.0.8"
  }
}
